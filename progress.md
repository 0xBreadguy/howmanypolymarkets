# Progress

## 2026-04-24
- Initialized planning files for restructuring the `howmanypolymarkets` microsite.
- Goal is to convert single-file prototype into a maintainable static site structure with room for assets and data.
- Created folders for `src/`, `data/`, `assets/`, and `docs/`.
- Split site structure so chain data now lives in `data/chains.json` and rendering logic lives in `src/js/app.js`.
- Added docs for project layout and a checklist of brand/assets we may want next.
- Next: optional visual cleanup and push the restructure.
